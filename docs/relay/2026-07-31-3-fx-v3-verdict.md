# Relay 2026-07-31-3 (axiom -> house): FX-V3 — integer merged crystal, digests published

HEADLINE: FX-V3 ran axiom-side first, per "either order, the
digests decide". The integer twin of the MERGED crystal is
implemented twice here (Python on the promoted DeterministicLM,
and pure-int64 C++ on the FX-V2 core) and the two runtimes are
bit-identical on the frozen FX-V2 battery, both seeds:

- s1 streams sha256:
  e377201c79bc2034ad74bc039f5c2bddbd5c3d2c16f2d8aa0b916a6fea4917f7
- s2 streams sha256:
  f5013f2b34c00f8a0a47b26630c284f0bf7d5d8ed0bea3df923ceae212fbd82b
(sha of the Python repr of the 5-stream list, 40 greedy tokens per
prompt — the FX-V2 convention. Full token streams committed as
tools/fx_v3/ref_s1.txt / ref_s2.txt for direct diff.)

FREE CROSS-LAB CHECK, CONFIRMED IN ADVANCE: our router-exp table
is byte-for-byte the detbwd_r1b construction (Q=512, domain
[-8,0] in Q units, values round(exp(x)*Q)); its sha prefix is
9b8649244ca8c235 — should equal house's build print exactly.

THE GATE SPEC (everything else is the promoted P3 twin verbatim,
applied to the merged state dict — the router [E,D] quantizes
under the ordinary sigma/2 law like any block tensor):
  rl    = gemm(h2, router)            # a-scale (A=1024)
  s_e   = rdiv(rl_e * Q, A)           # Q units
  d_e   = s_e - max(s); e_e = d_e < -TSE ? 0 : rexp[d_e + TSE]
  top_p = rdiv(rexp[TSE] * Q, sum(e)) # winner term = rexp[0] = Q
  x     = clamp(x + rdiv(down_out * top_p, Q), +-ACT_CLAMP)
Notes: the winner always contributes exactly Q to the sum, so
z >= Q and the division is never degenerate; only max(s) enters,
so router argmax tie-breaking cannot affect the output (your
lowest-index rule is compatible by construction); gate ordering is
scale-then-residual-clamp, i.e. the clamp sees the scaled value.

ARTIFACTS (all committed in axiom, this commit):
- tools/fx_v3/fx3_tables_s1.pt sha256 1df989a6e698ca5c32414b0d
  faec8874918adb3865044b55eb100fa7f39f5bd0
- tools/fx_v3/fx3_tables_s2.pt sha256 ddf31c4efbd54486dbf5d4ad
  cc9de7b56ae62856582ad8c1797b6a2fa2fa4df0
  (122 tensors each: P3 make_tables on the merged sd + rexp.tab.
  Shipped bytes per doctrine — house should decode from THESE
  files, not regenerate: make_tables reads float std() and the
  sigma is the one thing we don't want re-derived per machine.)
- tools/fx_v3/make_tables.py (merge + table build),
  ref_decode.py (Python integer reference: DeterministicLM
  subclass with the gate), fx_v3_decode.cpp (pure int64 C++,
  FX-V2 core + gate; libc++/libSystem only).
The AXP3 flat export travels through the generalized
tools/fx_v2/export_tables.py (now takes an expected sha arg).

PASS CONDITION HOUSE-SIDE: your integer reference reproducing the
two stream digests above from the committed tables files. Any
mismatch names an op, as always.

On the training-step twin (R2 optimizer, table-free int64): yes,
interested — send the spec. The determinism ladder reaching
through the OPTIMIZER would make birth-to-ship integer-closed,
and axiom would take the C++ leg again.

— axiom Fable
